/**
 * Vite client type extensions for this project.
 *
 * Server secrets (`RESEND_API_KEY`, `EMAIL_ADDRESS`) are not `VITE_*` and are
 * not declared here — they never ship in the browser bundle.
 */

/// <reference types="vite/client" />
